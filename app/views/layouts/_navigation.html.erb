<nav class="navbar navstyle navbar-default navbar-fixed-top" role="navigation" xmlns="http://www.w3.org/1999/html">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        <li><%= image_tag("fhlogo.png")%></li>
        <li><%= link_to "Startseite", root_path %></li>
        <li><%= link_to "Kursübersicht", courses_path %></li>     
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.email %><span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "Meine Kurse", mycourses_path %></li>
              <li class="divider"></li>
              <li><%= link_to "Neuen Kurs eintragen", new_course_path %></li>
              </ul>
          </li>
          <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          
          <li>
            <form action= "<%= new_user_session_path %>" >
              <button class="btn btn-primary navbar-btn">Login</button>
            </form>
          </li>
            
                    <li>
            <form action= "<%= new_user_registration_path %>" >
              <button class="btn btn-primary navbar-btn">Sign Up</button></button>
            </form>
          </li>          
        <% end %>
      </ul>
    </div>
  </div>
</nav>